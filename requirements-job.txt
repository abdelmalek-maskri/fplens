# What `python -m job.snapshot` needs: fetch live data, run the models, write
# the JSON the site reads. Heavier than the API because this is the half that
# actually loads models.
#
# Use requirements.txt for training, requirements-api.txt for serving.
#
# Deliberately excluded:
#   torch, transformers, spacy  Guardian sentiment falls back to keyword scoring
#                               and regex player linking without them, at the
#                               cost of ~400MB of wheels and a ~500MB download.
#
# Pins match requirements.txt.

pandas==2.3.3
numpy==2.2.6
scipy==1.15.3

# Loading the pickled models and their estimators
joblib==1.5.3
scikit-learn==1.7.2
lightgbm==4.6.0
xgboost==3.2.0

# Per-player SHAP breakdowns in players.json. Pulls numba and llvmlite (~130MB).
shap==0.49.1

# External data: FPL and Guardian over HTTP, Understat via its async client
requests==2.32.5
aiohttp==3.8.3
understat==0.1.14
beautifulsoup4==4.11.1
python-dotenv==1.2.2
